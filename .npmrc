# Mirror the lexical monorepo's linker so a single React/lexical instance is
# hoisted flat into node_modules (avoids duplicate-React context bugs and keeps
# cross-package source resolution simple when bundling TypeScript directly).
node-linker=hoisted
# Some optional transitive deps ship native onnxruntime binaries we never use.
onnxruntime-node-install=skip
