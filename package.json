{
  "name": "vendored-lexical-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "pnpm --filter lexical-source-demo dev",
    "build": "pnpm --filter lexical-source-demo build",
    "preview": "pnpm --filter lexical-source-demo preview",
    "tsc": "pnpm --filter lexical-source-demo run tsc",
    "lexical:pull": "git subtree pull --prefix vendor/lexical lexical-upstream claude/kind-johnson-RexEG --squash"
  }
}
